export function makeHeadsOrTails(zeroOrOne) {
    if (zeroOrOne === 1) {
        return 'heads';
    } 

    return 'tails';
}

// export const makeHeadsOrTails = (zeroOrOne) => {
//     return zeroOrOne ? 'heads' : 'tails';
// };
